- Carregar os dados de vértices, normais e coordenadas de texturas e seus indices.
- Em um loop, retirar: um vértice, uma normal e um coordenada de textura.
- Salvar em um strutura que descreve um ponto e seus atributos.
- Os critério para adicionar ao buffer é:
	- Se o buffer estiver vazio, insira*.
	- Se a estrutura atual não estiver no buffer, coloque-a (caso em que i == buffer.size()*)
	- Se qualquer atributo da estrutura diferir, esse é um novo atributo portanto deve ser inserido no buffer*
	- Caso a estrutura já esteja no buffer, apenas salve a posição em qual a estrutura está armazenada no buffer no fluxo de índices.

*(o fluxo de indices recebe buffer.size() antes da inserção)
